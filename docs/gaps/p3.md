# Person 3 Contract Gaps

Logged during the P3 → CloudOps-merged integration (P1+P2 already merged; P3 dropped in from
`Code-Crafters-BIT-N-BUILD-26-p3-c3-ai-hardening`). Structural merge is complete and clean (see
merge notes below); the items here are **behavioral** gaps found while wiring the code, kept open
for Person 3 to close.

- [2026-09-20] GAP: `app/seeds/data.py` (`ORDER=20`) has an empty `run(db)` (`pass`). No rows are
  written to `resources`, `metrics`, `costs`, `price_catalog`, `anomalies`, or `recommendations` on
  seed. · found by: merge review · proposal: implement using the 14-resource fixture already in
  `app/adapters/mock_adapter.py::SEEDED_RESOURCES` + `price_catalog.py`, per §11 (7-day 5-min metric
  backfill, 30-day costs, idempotent by deterministic `uuid5`). · status: open
- [2026-09-20] GAP: `app/api/v1/resources.py`, `metrics.py`, `costs.py`, `anomalies.py`,
  `dashboard.py`, `billing.py` read from in-memory/hardcoded data (`SEEDED_RESOURCES`, literal
  numbers) instead of the DB (`app.db.models`), so per §16.5 P3 is not yet writing/reading the
  tables it owns. Works for a demo boot with no DB rows, but resources created via real
  `cloud-accounts` sync or actions taken via the P2 orchestrator won't be reflected. · found by:
  merge review · proposal: swap in `db: AsyncSession = Depends(get_db)` + SQLAlchemy queries once
  data.py seeds real rows. · status: open
- [2026-09-20] GAP: `app/services/ingestion.py::sync_account` fetches resources/metrics from the
  adapter and publishes WS events, but never upserts them into `resources`/`metrics`, and never
  calls the analytics/recommendation engine (both noted as TODO in its own docstring). · found by:
  merge review · proposal: add the upsert + `analytics.py`/`recommendation.py` calls at the end of
  `sync_account`. · status: open
- [2026-09-20] GAP: `app/services/cost.py::budget_usage` returns a fixed
  `(477.52, 920.00)` regardless of `budget_id`. · found by: merge review · proposal: compute from
  real `costs` rows once seeded. · status: open

## Merge notes (informational, not gaps)
- P3's zip nested everything under `backend/app/...`; this repo keeps `app/` at the root (build
  context for `backend/Dockerfile` is `.`), so files were copied to `app/...` directly — no import
  changes needed since all P3 code already imports via the bare `app.*` package.
- `app/schemas/base.py` existed in both drops and is byte-for-byte equivalent (aside from
  docstrings/whitespace); kept the existing one, did not overwrite.
- Fixed `app/seed.py` to auto-discover seed modules under `app/seeds/` sorted by `ORDER` (per
  §16.4/§11), instead of only calling `seeds/core.py`. Previously `seeds/data.py` (P3, `ORDER=20`)
  was never invoked at all.
- All `provide()` calls (`cost_engine`, `ingestion`, `adapters`) register cleanly; verified via
  static analysis (no live network/pip in this sandbox to actually boot `docker compose`, so this
  hasn't been smoke-tested end-to-end — do that first).
- No router path collisions between P2's and P3's `app/api/v1/*.py` (65 routes total, checked
  programmatically).
- Tests copied to `backend/tests/p3/` (P2's convention is `backend/tests/p2/`); `pytest.ini` has
  `pythonpath = .` so imports resolve from repo root either way.
- No new third-party dependencies required — P3 code only uses `fastapi`, `pydantic`, `httpx`,
  `numpy`, all already pinned in `requirements.txt`.
